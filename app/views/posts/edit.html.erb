<%= form_with(model: @post, local: true) do |post| %>
 

  <%= post.label :title %>
  <%= post.text_field :title %>
  <%= post.label :content %>
  <%= post.text_field :content %>
  <%= post.submit "Create", class: "btn btn-outline-success btn-sm" %>
<% end %>
